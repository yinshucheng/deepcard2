# NNN — <标题>

- **状态**: proposed
- **优先级**: 高 / 中 / 低
- **作者**: <name>
- **创建**: YYYY-MM-DD
- **依赖**: <其它 spec 编号，或「无」>
- **Supersedes**: <被本 spec 取代的编号，或删掉本行>

## 背景 / 动机

为什么要做这个？解决什么痛点？不做会怎样？

## 目标

一句话说清这个特性要达成什么。

### 非目标

明确**这次不做**的东西，防止范围蔓延。

## 需求

用户视角的行为描述（可用「作为…我想要…以便…」）。

## 验收标准

可勾选、可验证的清单。完成即逐条打勾。每条尽量写成可执行的 Happy Path（curl / 测试 / 浏览器操作 + 期望结果），既是验收用例也是回归用例。

- [ ] …
- [ ] …

## 技术方案

接口契约、Zod schema、Drizzle schema 改动、新增/改动的 Operation（registry）、Service 方法、与现有架构的对接点。**尤其要说明如何不破坏架构基线**（见 `000-architecture.md`）：是否新增 operation 而非绕过 registry、service 是否保持零协议依赖。

## 任务拆解

实现步骤，按依赖顺序排列。对应 CLAUDE.md「分阶段铁律」：每步尽量自带 happy path。

1. …
2. …

## 风险 / 待定

已知风险、需要进一步确认的开放问题。

## 实现记录

（完成后填）落地的文件、提交、与原方案的偏差。
